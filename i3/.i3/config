# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec xterm

# kill focused window
bindsym Control+$mod+Shift+Q kill

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym Control+$mod+s layout stacking
bindsym Control+$mod+w layout tabbed
bindsym Control+$mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# TODO: This does not seem to work. Investigate
bindsym $mod+Space focus mode_toggle

# focus the parent container
bindsym Control+$mod+a focus parent

bindsym $mod+q workspace 1
bindsym $mod+w workspace 2
bindsym $mod+e workspace 3
bindsym $mod+a workspace 4
bindsym $mod+s workspace 5
bindsym $mod+d workspace 6
bindsym $mod+z workspace 7
bindsym $mod+x workspace 8
bindsym $mod+c workspace 9

bindsym $mod+u workspace 10
bindsym $mod+i workspace 11
bindsym $mod+o workspace 12
bindsym $mod+j workspace 13
bindsym $mod+k workspace 14
bindsym $mod+l workspace 15
bindsym $mod+m workspace 16
bindsym $mod+comma workspace 17
bindsym $mod+period workspace 18

bindsym $mod+Shift+Q move container to workspace 1
bindsym $mod+Shift+W move container to workspace 2
bindsym $mod+Shift+E move container to workspace 3
bindsym $mod+Shift+A move container to workspace 4
bindsym $mod+Shift+S move container to workspace 5
bindsym $mod+Shift+D move container to workspace 6
bindsym $mod+Shift+Z move container to workspace 7
bindsym $mod+Shift+X move container to workspace 8
bindsym $mod+Shift+C move container to workspace 9

bindsym $mod+Shift+U move container to workspace 10
bindsym $mod+Shift+I move container to workspace 11
bindsym $mod+Shift+O move container to workspace 12
bindsym $mod+Shift+J move container to workspace 13
bindsym $mod+Shift+K move container to workspace 14
bindsym $mod+Shift+L move container to workspace 15
bindsym $mod+Shift+M move container to workspace 16
bindsym $mod+Shift+semicolon move container to workspace 17
bindsym $mod+Shift+colon move container to workspace 18

# Move workspace to left output
bindsym $mod+Control+Left move workspace to output left
bindsym $mod+Control+Right move workspace to output right

# switch wi3 workspace with rofi
bindsym $mod+F5 exec --no-startup-id /home/magnus/scripts/i3_switch_workspace.sh

# monitor layout with rofi
bindsym $mod+F4 exec --no-startup-id /home/magnus/scripts/monitor-layout.sh

# Pulse Audio controls
bindsym $mod+Control+Shift+Up exec /home/magnus/scripts/volume-control.sh up 5
bindsym $mod+Control+Shift+Down exec /home/magnus/scripts/volume-control.sh down 5
bindsym $mod+Control+Shift+m exec /home/magnus/scripts/volume-control.sh mute
bindsym $mod+Control+Shift+Left exec /home/magnus/scripts/volume-control.sh down 2
bindsym $mod+Control+Shift+Right exec /home/magnus/scripts/volume-control.sh up 2

# controls for Spotify (via mopidy and ncmpcpp)
bindsym $mod+Control+Shift+Pause exec ncmpcpp toggle
bindsym $mod+Control+Shift+p exec ncmpcpp prev
bindsym $mod+Control+Shift+n exec ncmpcpp next

# reload the configuration file
bindsym Control+$mod+Shift+C reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart

# exit i3 (logs you out of your X session)
bindsym Control+$mod+Shift+E exit

bindsym $mod+Control+l exec --no-startup-id "/usr/bin/i3lock --color=3f3f3f"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym odiaeresis resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id /home/magnus/scripts/i3exit lock, mode "default"
    bindsym e exec --no-startup-id /home/magnus/scripts/i3exit logout, mode "default"
    bindsym s exec --no-startup-id /home/magnus/scripts/i3exit suspend, mode "default"
    bindsym h exec --no-startup-id /home/magnus/scripts/i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id /home/magnus/scripts/i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id /home/magnus/scripts/i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Pause mode "$mode_system"

bindsym --release $mod+Shift+v exec --no-startup-id import /tmp/screen.png

bindsym $mod+Control+Return exec --no-startup-id emacsclient -c -n

bindsym $mod+Shift+Return exec --no-startup-id /home/magnus/scripts/conkeror

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# Colors
# class border backgr. text
client.focused #525252 #1E1E1E #F5DEB3
client.focused_inactive #333333 #0F0F0F #E3CCA1
client.unfocused #333333 #010101 #85753A
client.urgent #856E23 #900000 #F5DEB3

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    status_command i3blocks -c ~/.i3/i3blocks.conf
    # status_command i3status
    font pango:Cousine, FontAwesome 8
    colors {
    	   background #3F3F3F
    }
}

focus_follows_mouse no
workspace_auto_back_and_forth yes

# window rules
for_window [class="Emacs"] border none
for_window [class="XTerm"] border none
for_window [class="libreoffice"] border none

# Float pinentry windows
for_window [title="pinentry"] floating enable

# Float pinentry windows
for_window [title="qalculate!"] floating enable

# Autorun
exec xrdb ~/.Xresources
exec --no-startup-id /usr/bin/nitrogen --restore &
exec --no-startup-id /usr/bin/xmodmap /home/magnus/.xmodmap
exec --no-startup-id /usr/bin/autocutsel -f -s PRIMARY
exec --no-startup-id /usr/bin/autocutsel -f -s CLIPBOARD
exec --no-startup-id /usr/bin/autocutsel -f -s SECONDARY
exec --no-startup-id /home/magnus/scripts/start-syncthing.sh &
exec --no-startup-id /usr/bin/unclutter -idle 1 &
exec --no-startup-id /usr/bin/xautolock -time 10 -locker "/usr/bin/i3lock --color=3f3f3f" -corners +-00 -cornerdelay 4 -cornerredelay 4 &
exec --no-startup-id /home/magnus/scripts/start-redshift.sh &
exec --no-startup-id mopidy --config .mopidy.conf
exec --no-startup-id emacs --daemon
exec --no-startup-id clipit
exec --no-startup-id dunst
exec --no-startup-id rofi -key-window SuperL+F3 -key-run SuperL+F1 -key-ssh SuperL+F2
